SELECT*
FROM EMPLOYEES;

SELECT SALARY
FROM EMPLOYEES;

SELECT DISTINCT SALARY
FROM EMPLOYEES
ORDER BY SALARY DESC
LIMIT 2,1;

-- OR
SELECT DISTINCT SALARY
FROM EMPLOYEES
ORDER BY SALARY DESC
LIMIT 1
OFFSET 2;

SELECT *
FROM EMPLOYEES
WHERE SALARY = (
	SELECT DISTINCT SALARY
	FROM EMPLOYEES
	ORDER BY SALARY DESC
	LIMIT 2,1
    );

	-- OR
SELECT*
FROM EMPLOYEES
WHERE SALARY = (
	SELECT DISTINCT SALARY
	FROM EMPLOYEES
	ORDER BY SALARY DESC
	LIMIT 1
	OFFSET 2);